---
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
  /** no header/footer, full-window body (used by /play/) */
  bare?: boolean;
}

const {
  title,
  description = 'Solve mazes that are really in three dimensions: boxes, spheres, pyramids and more, in 5 sizes and 23 graphic themes, with a new free maze every day. Tap to run along the pipes, drag to turn the camera.',
  bare = false,
} = Astro.props;

const base = import.meta.env.BASE_URL; // "/pipe-maze-3d/"
const url = new URL(Astro.url.pathname, Astro.site);
const og = new URL(`${base}images/og.jpg`, Astro.site);
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="theme-color" content="#07102a" />
    <link rel="icon" type="image/png" href={`${base}favicon.png`} />
    <link rel="apple-touch-icon" href={`${base}favicon.png`} />
    <link rel="canonical" href={url} />
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="Pipe Maze 3D" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={url} />
    <meta property="og:image" content={og} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:site" content="@kuragestudios" />
  </head>
  <body class:list={[{ bare }]}>
    {!bare && (
      <header class="site-header">
        <div class="container header-inner">
          <a class="brand" href={base}>
            <img src={`${base}images/icon.png`} alt="" width="36" height="36" />
            <span>Pipe Maze 3D</span>
          </a>
          <nav>
            <a href={`${base}#features`}>Features</a>
            <a href={`${base}#shapes`}>Shapes</a>
            <a href={`${base}#themes`}>Themes</a>
            <a href={`${base}#how-to-play`}>How to play</a>
            <a href={`${base}#download`}>Download</a>
            <a class="btn btn-primary btn-small" href={`${base}play/`}>Play</a>
          </nav>
        </div>
      </header>
    )}

    <slot />

    {!bare && (
      <footer class="site-footer">
        <div class="container footer-inner">
          <p>
            © {new Date().getFullYear()} <a href="https://www.kuragestudios.com" target="_blank" rel="noopener">Kurage Studios</a> · made in Japan
          </p>
          <p class="muted">
            <a href="https://www.kuragestudios.com/press-kit/pipe-maze-3d/" target="_blank" rel="noopener">Press kit</a> ·
            <a href="https://www.youtube.com/watch?v=ea3AvLVRcl8" target="_blank" rel="noopener">Trailer</a> ·
            <a href="mailto:kuragestudios@gmail.com">Contact</a>
          </p>
          <p class="muted">
            Also in your browser: <a href="https://kuragestudios.github.io/kid-odysseus/" target="_blank" rel="noopener">Kid Odysseus</a> ·
            <a href="https://kuragestudios.github.io/just-maths-training/" target="_blank" rel="noopener">Just Maths Training</a>
          </p>
        </div>
      </footer>
    )}
  </body>
</html>

<style>
  body.bare { height: 100dvh; overflow: hidden; }

  .site-header {
    position: sticky;
    top: 0;
    z-index: 50;
    background: rgba(7, 16, 42, 0.85);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid var(--line);
  }
  .header-inner { display: flex; align-items: center; justify-content: space-between; gap: 1rem; padding: 0.7rem 0; }
  .brand { display: flex; align-items: center; gap: 0.6rem; color: var(--text); font-weight: 700; }
  .brand img { border-radius: 8px; }
  .brand:hover { text-decoration: none; }
  nav { display: flex; align-items: center; gap: 1.2rem; font-size: 0.95rem; }
  nav a { color: var(--muted); }
  nav a:hover { color: var(--text); text-decoration: none; }
  nav a.btn-small { padding: 0.55em 1.1em; font-size: 0.75rem; color: var(--bg); }
  nav a.btn-small:hover { color: var(--bg); }
  @media (max-width: 640px) { nav a:not(.btn) { display: none; } }

  .site-footer { border-top: 1px solid var(--line); padding: 2rem 0; font-size: 0.9rem; }
  .footer-inner { display: flex; flex-wrap: wrap; justify-content: space-between; gap: 1rem; }
  .footer-inner p { margin: 0; }
</style>
